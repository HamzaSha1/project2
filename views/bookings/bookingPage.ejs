<%- include('../partials/header') %>

<% if(user.role === "Nurse") { %>
    <% user.booking.forEach(function(r) { %>
        <% if(user?._id.toString() === r.nurseId.toString()) { %>
            <p><%= r.customerName %></p>
            <p><%= r.customerEmail %></p>
            <p><%= r.nursePrice %></p>
            <p><%= r.bookingDate.toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: '2-digit', year: 'numeric' }) %></p>
            <p><%= r.bookingTime %></p>
            <p><%= r.bookingNotes %></p>
            <p><%= r.customerHouse %></p>
            <p><%= r.customerBlock %></p>
            <p><%= r.customerRoad %></p>
        <% } %>
    <% }); %>
<% } else if(user.role === 'Customer') { %>
    <% user.booking.forEach(function(r) { %>
        <% if(user?._id.toString() === r.customerId.toString()) { %>
            <p><%= r.nurseName %></p>
            <p><%= r.nurseEmail %></p>
            <p><%= r.nursePrice %></p>
            <p><%= r.bookingDate.toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: '2-digit', year: 'numeric' }) %></p>
            <p><%= r.bookingTime %></p>
            <p><%= r.bookingNotes %></p>
        <% } %>
    <% }); %>
<% } %>

<%- include('../partials/footer') %>