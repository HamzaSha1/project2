<%- include('../partials/header') %>

<table id="list">
    <thead>
      <tr>
        <th>Nurse Name</th>
        <th>Price</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% filteredNurses.forEach(function(n) { %>
        <tr>
          <td><a href="/clients/clientNurseDetails/<%= n._id %>"><%= n.name %></a></td>
          <td><%= n.price %></td>
          <td><a href="/clients/clientBookingPage/<%= n._id %>">Quick Book</a></td>
        </tr>
        <% }); %>
      </tbody>
    </table>

<%- include('../partials/footer') %>